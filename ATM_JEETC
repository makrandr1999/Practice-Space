#include <stdio.h>
int main(void) {
	int w;
	float bal;
	scanf("%d %f",&w,&bal);
	if(w+0.5<=bal && w%5==0)
	{
	    bal=bal-w-0.5;
	    printf("%0.2f",bal);
	}
	else
	{
	    printf("%0.2f",bal);
	}
	return 0;
}
